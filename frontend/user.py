class User:
        def __init__(self, user_id, mail_address, password, nick_name, last_login):
                self.user_id = user_id
                self.mail_address = mail_address
                self.pass_word = password
                self.nick_name = nick_name
                self.last_login = last_login
